# from app.services.device_service import configure_device

# def test_configure_device():
#     # Prepare test data
#     device = {"id": 1, "status": "READY", "temperature": -1}

#     # Call the function to configure the device
#     configure_device(device)

#     # Assert the device status and temperature are updated
#     assert device["status"] == "ACTIVE"
#     assert 0 <= device["temperature"] <= 10
